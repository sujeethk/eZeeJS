'use strict';

//var winston = require('winston');

var validFormats = ['combined', 'common', 'dev', 'short', 'tiny'];

module.exports.setup = function(loggerConfig) {
  return new Promise(function(resolve, reject) {
    //Setup winston logger
    resolve();
  });
}
